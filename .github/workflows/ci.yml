name: CI

on: [push]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build app
      run: fastlane build

    - name: Run tests
      run: fastlane test
